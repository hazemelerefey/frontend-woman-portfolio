/**
 * Single source of truth for the résumé download.
 * Drop the actual file at `public/Shahd-Khairy-CV.pdf` — the filename below
 * must match it exactly.
 */
export const RESUME_URL = '/Shahd-Khairy-CV.pdf';
